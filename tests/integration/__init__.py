"""Integration tests for RedHouse aggregation pipeline.

These tests connect to actual InfluxDB and use *_test buckets.
Run only during development, not during deployment.

Usage:
    pytest tests/integration/ -v
"""
